/**
 * SimpleAICommand - Alias for AICommand for backward compatibility
 */

import { AICommand } from './ai-command';

/**
 * SimpleAICommand is an alias for AICommand
 * This is for backward compatibility with existing tests
 */
export class SimpleAICommand extends AICommand {
  // This class exists for backward compatibility
  // All functionality is inherited from AICommand
}
